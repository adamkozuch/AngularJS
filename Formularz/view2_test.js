'use strict';

describe('myApp.Formularz module', function() {

  beforeEach(module('myApp.Formularz'));

  describe('Formularz controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});